
""" Observer Pattern """
# `Observer` (`Listener`)
# subject and observer: subject does things, changes things, and notifies
#   observers of any changes that happened; Event-Menegement System;
